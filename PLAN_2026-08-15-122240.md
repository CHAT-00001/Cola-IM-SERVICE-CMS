// PLAN_2026-08-15-122240.md
// 架构重构与多应用/多租户（App ID & Cola Basic）实施计划
// 2026/8/15 12:22 Created.

////////

# 📋 架构重构与多应用/多租户（App ID & Cola Basic）实施计划

- **文档编号**: PLAN_2026-08-15-122240
- **创建日期**: 2026-08-15
- **当前状态**: 实施中（Act 模式）

---

## 🎯 核心目标
1. **模块演进**：将原有的杂项第三方模块 `cola_three` 重构升级为系统级核心基础大模块 **`cola_basic`**。
2. **应用管理**：在 `cola_basic` 中引入全局 `app`（应用/租户）管理实体与仓储，统一管理所有子业务模块（如 `short_video`, `cola_live` 等）。
3. **存储隔离**：在 `cola_fs` 的 `bucket`（存储桶）中增加 `app_id` 字段，实现“一个业务一个 Bucket、按应用隔离存储”。
4. **智能组装**：在 `cola_video` 的视频组装器中支持基于 `app_id` 动态获取 CDN 域名，并完美兼容 PHP 时代的历史债务（相对路径与绝对 URL 的智能归一化）。

---

## 🛠️ 实施步骤详解

### 第一阶段：模块重构（`cola_three` ➔ `cola_basic`）
1. 重命名或调整 `cola_three` 目录及相关 Cargo 依赖为 `cola_basic`。
2. 在工作区 `Cargo.toml` 中更新成员及引用关系。

### 第二阶段：引入全局 `app` 统一管理
1. **数据契约 (`cola_data`)**：
   - 创建 `AppEntity` / `AppCommand` / `AppQuery`。
   - 定义 `app` 数据表结构（`app_id`, `name`, `status`, `created_at`, `updated_at`）。
2. **仓储与适配器 (`repository` & `repo_adapter`)**：
   - 实现 `AppRepo`（支持按 `app_id` 查询应用基本信息与状态）。
3. **端口暴露 (`port`)**：
   - 在 `cola_basic` / `port` 中暴露应用管理端口。

### 第三阶段：`cola_fs` 存储桶绑定 `app_id`
1. **实体升级**：
   - 修改 `cola_data/src/cola_fs/entity/bucket.rs` 中的 `BucketEntity` 和 `BUCKET_COLUMNS`，正式引入 `app_id: Option<String>` 字段。
2. **仓储对齐**：
   - 更新 `repository/src/cola_fs/pg/bucket.rs` 的 SQL，支持存储桶与 `app_id` 的关联读写。

### 第四阶段：`cola_video` 组装器集成 CDN 与 PHP 历史债务兼容
1. **动态 CDN 查询**：
   - 在视频组装过程中，根据当前业务的 `app_id`（如 `"short_video"`）通过 `cola_fs` 端口获取对应的 CDN 加速域名。
2. **URL 归一化处理（PHP 历史债务兼容）**：
   - 编写工具函数处理 `href` 和 `thumb`：
     - 若已是 `http://` / `https://` / `//` 开头，保持原样。
     - 若是相对路径，自动拼接对应的 CDN 基础域名。

//////// END
