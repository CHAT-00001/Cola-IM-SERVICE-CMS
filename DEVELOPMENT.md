# 📁 DEVELOPMENT DOC

## 🗂️ 文件目录架构 
# 六边形 整洁模式

| 模块               | 说明                               |
|------------------|----------------------------------|
| **GATE 网关**      | 处理 HTTP 请求与路由分发                  |
| **APP 状态机/配置**   | 管理数据库连接池、应用状态、多服务启动等             |
| **COLA DATA**    | 防止依赖循环, 抽离干净中立的data model create |
| **COLA_AUTH**    | 验证中心, 登录/注册/IP/设备.. 检查           |
| **COLA_DYNAMIC** | UGC 可乐动态                         |
| **COLA_LIVE**    | UGC 可乐直播                         |
| **COLA_MUSIC**   | UGC 可乐音乐                         |
| **COLA_RICK**    | - 风控模块                           |
| **COLA_USER**    | BASIC 可乐用户中心 / 权限 / 用户关系         |
| **COLA_VIDEO**   | UGC 短视频                          |
| **COLA_POST**    | UGC 博文                           |
| **COLA_SHOP**    | 电子商务                             |
| **IM**           | 聊天IM的客户端                         |

---

## ⚙️ API 路由器 / HANDLER

> 负责接收请求、参数校验、调用 App 层、返回响应。

---

## 🧠 APP 应用状态机

- 数据库连接池管理
- 应用状态维护
- 多服务协同启动

---

## 🧩 CORE / 应用名称（LIB）

| 层级              | 职责                       |
|-----------------|--------------------------|
| **API 层**       | 应用权限编排、统一响应壳 `AppData`   |
| **CASE 层**      | 逻辑流程编排，调用工具函数与 Port 接口   |
| **Assembler 层** | 多维数据拼接组装，供 Biz 调用        |
| **Port 层**      | 定义 trait 接口，供 Bridge 库实现 |
| **Kits**        | 其他辅助工具（备用）               |

### 🎬 UGC APP 层用例
# 抽象用例, 包含子模块

| 用例          | 说明                        |
|-------------|---------------------------|
| `ADD`       | 创建 / 发布 / 编辑 / 删除  / 权限修改 |
| `VIEW`      | 浏览                        |
| `LIKE`      | 点赞/不喜欢 2个模型               |
| `COLLECT`   | 收藏                        |
| `COMMENT`   | 评论                        |
| `DANMAKU`   | 弹幕                        |
| `SHARE`     | 分享                        |
| `RECOMMEND` | 推荐                        |
| `HOTLIST`   | 推荐                        |
| `RECOMMEND` | 推荐                        |
| `REPORT`    | 举报                        |
| `BUY`       | 购买                        |
| `HOME`      | 公共的前台应用抽象                 |
| `FEED`      | 私密的关于我的用例                 |
| `MINE`      | 己方的内容接口 创作者中心             |

---

## 🔌 BRIDGE / 应用名称（LIB）

| 模块          | 说明                                       |
|-------------|------------------------------------------|
| **adapter** | 自适应仓储层（CACHE > DB > GRPC > MOCK），保证程序稳定性 |

---

## 📦 DATA / 应用名称（LIB）

| 模块         | 说明                     |
|------------|------------------------|
| `command`  | 创建命令                   |
| `entity`   | 数据表实体映射                |
| `info`     | 从实体构造元数据对象             |
| `model/vo` | 从 Info 组装视图对象 `XxxxVO` |

---

## 🗃️ REPO / 应用名称（LIB）

| 模块      | 状态     | 说明             |
|---------|--------|----------------|
| `redis` | ✅ 已用   | 缓存层            |
| `pg`    | ✅ 已用   | PostgreSQL 仓储层 |
| `grpc`  | 🚧 未开发 | 远程调用           |
| `mock`  | 🚧 未开发 | 模拟数据           |

---

## 🔄 响应顺序流程

| 步骤 | 组件               |
|----|------------------|
| 1  | `Entity`（表实体）    |
| 2  | `Info`（Redis 缓存） |
| 3  | `VideoVo`（聚合展示）  |
| 4  | `Response`（原始响应） |
| 5  | `AppData`（统一响应壳） |

**流程图：**


## 🚀 请求穿透层级,自上而下

| 步骤 | 组件                                  |
|----|-------------------------------------|
| 1  | `公网`（表实体）                           |
| 2  | `GATEWAY`（web框架,解析注入参数）             |
| 3  | `API`（接口层,组装AppData响应壳）             |
| 4  | `CASE`（用例编排）                        |
| 5  | `PORT`（服务端口）                        |
| 6  | `ADAPTER`（适配器）                      |
| 7  | `REPOSITORY`（仓储层/pg/redis/rpc/mock） |
| 8  | `PORT`（统一响应壳）                       |

---